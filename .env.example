DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET=

# Painel /admin/invites (gera códigos de convite para signup)
BASIC_AUTH_USER=
BASIC_AUTH_PASS=
